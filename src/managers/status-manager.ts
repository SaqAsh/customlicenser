export class StatusManager {
	// Basic implementation - can be expanded later
	constructor() {}

	start(): Promise<void> {
		return Promise.resolve();
	}

	stop(): Promise<void> {
		return Promise.resolve();
	}
}
